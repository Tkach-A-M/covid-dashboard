{
    //"extends":["airbnb/base"],
    "extends":[""],
    "env": {
      "browser": true,
      "node": true
    },
    "rules": {
      "linebreak-style": ["error", NODE_ENV === 'prod' ? "unix" : "windows"]
    }
}